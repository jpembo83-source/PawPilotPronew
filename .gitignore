.superpowers/
.DS_Store
.local/
.config/
.agents/
.planning/
.netlify/
**/.local/
**/.config/
**/.netlify/
**/supabase/.temp/
*.bin
zipFile.zip
.worktrees/

# Dependency/tool caches — never content
node_modules/

# Pipeline DB dumps — NEVER commit prod data (sanitised or raw). See
# scripts/pipeline/README.md. Belt-and-braces so a stray dump can't be staged.
*.raw.sql
*.sanitised.sql
prod-data*.sql
reset.sql
